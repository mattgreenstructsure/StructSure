# StructSure website

Static marketing homepage for StructSure. The `preview` branch is intended for review through GitHub Pages; `main` remains the approved baseline until changes are accepted.

## Preview publishing

In **Settings → Pages → Build and deployment**, set **Source** to **GitHub Actions**. Each push to `preview` publishes the review site without changing the real StructSure domain.
